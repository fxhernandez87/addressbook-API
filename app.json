{
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "env": {
    "PORT": {
      "required": true
    },
    "ENV": {
      "required": true
    },
    "MONGODB_URI": {
      "required": true
    },
    "FIREBASE_APIKEY": {
      "required": true
    },
    "JWT_SECRET": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "name": "pipeline-heroku",
  "scripts": {
    "test": "mocha --recursive"
  },
  "stack": "heroku-18",
  "environments": {
    "test": {
      "scripts": {
        "test": "mocha --recursive"
      },
      "formation": {
        "test": {
          "quantity": 1,
          "size": "free"
        }
      }
    }
  }
}
